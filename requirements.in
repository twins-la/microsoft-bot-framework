# Top-level third-party dependencies for the Microsoft Bot Framework twin
# (both the main package and the local host package). The local path
# packages (twins-microsoft-bot-framework[-local], twins-local) are NOT
# listed here — they are installed editable from the checkout.
#
# Regenerate the lock after editing:  uv pip compile requirements.in -o requirements.lock
flask>=3.0,<4.0
requests>=2.31,<3.0
PyJWT[crypto]>=2.8,<3.0
cryptography>=42.0
gunicorn>=22.0,<24.0

# Test/dev tooling (kept in the lock so CI installs a verified set).
pytest>=8.0
httpx>=0.27
